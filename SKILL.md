---
name: exam-answer-first-cn
description: 中文应试答题 Skill。先给一段可以直接抄写到试卷上的答卷版答案,再给展开解析;所有图默认用 matplotlib 生成真图并在对话里内联显示。适用于用户上传题目图片或文字后要求答题、给答案、做题、解析、讲解、批改时;仅询问知识点概念、要求润色文字或讨论学习方法时不触发。
---

# 先答卷后解析(中文应试答题)

产出的第一段是用户要誊写到试卷上的文字,第二段才是讲解。两段的目标和写法完全不同,不能混。

## 目标与边界

- 严格按照「答卷版答案 → 解析」的固定顺序输出,一次回答里两段都要有。
- 不编造教材页码、公式编号、题号、图号;图中连线关系读不清时明确说明,不猜。
- 不代写需要学生本人完成的实践记录、实验数据、署名材料;这类内容只给方法与要点。

## 输出契约(硬性规则)

每次回答必须包含且仅包含这两段,顺序固定,不合并、不调换、不省略:

```
一、答卷版答案
<可直接抄到试卷上的正文>

二、解析
<展开讲解>
```

- 第一段中禁止出现「解析/说明/注意/这是因为/由此可见」等字样,禁止写对解题过程的评论。
- **题目有多个独立小问时(如 (1)(2)(3)…),第一段必须按小问编号分点作答,一问一行/一段,顺序与题号一致——考卷上学生就是这么写的,不允许把所有小问揉成一大段。**
- **只有同一个小问内部的多个要素才合句**,例如题目写「指出被控对象、被控量和给定量」,这是一个小问里的三个要素,合并成「该系统的被控对象为电炉,被控量为炉温,给定量为给定电压」。
- **答卷段要像写在答题纸上的解题过程**:公式独立成行、步骤之间自然提行,不要挤成一大段密不透风的文字。缺了提行,答案再短也不像卷面。
- 两段之间用空行或分隔线隔开即可,不加额外寒暄。

## 一、答卷版答案的写法

唯一标准:**能拿分、抄得快、不啰嗦**。这是给阅卷老师看的东西,不是给学习者看的东西。

必须做到:

1. **分行书写,不用结构化标记**——按解题步骤自然提行:**一句文字、一行公式**,像手写在答题纸上那样(「由……」提行、「特征方程为」提行、公式单独一行、「故/解得」再提行);**不使用小标题、项目符号、编号列表(小问编号 (1)(2)(3) 除外)、加粗、表格**。提行是必要的,冗词是不必要的,两者不要混为一谈。图见第 5 条,公式分行见第 7 条。
2. **小问分点,要素合句**——按上一条的界线执行:跨小问分点,小问内合句。每个小问内部仍写成完整句子,不要写成「被控对象:电炉」这种标签冒号式。
3. **按题型分三档给内容**,每一档都只写得分点:
   - **原理 / 概念题**:只给判断、判据和结论句。
   - **计算题**(物理、电路、控制、信号等有数值代入的):按 **公式 → 代入数值 → 结果** 三步写,**中间的算术化简过程不写**(考试时那是草稿纸上的事),结果必须带单位。**题目只给符号、不给数值时,写「公式 → 化简后的符号结果式」,不强行代入。**
   - **证明 / 推导题**(数学证明、公式推导):保留必要的逻辑链,一行一个等式;重复的代数化简可以跳,但推理步骤不能跳。
   三档都不写对过程的评论(「这一步的思想是…」「注意不要忘记…」),也不出现「因为/由于/由此可见」这类解释性连接词。
4. **术语与符号与教材一致**——变量符号、上下标、单位按课程惯例书写,公式用标准形式给出。
5. **图一律生成真图并在对话里内联显示,但只在需要时画**(见「绘图规范」)。
   - **按需画图,不为题面配图**。只有三种情况才出图:(a) 题目明确要求画图(方框图、根轨迹、Bode 图、Nyquist 图、波形图、积分区域、立体图等);(b) 图是答案的必要组成部分(化简后的等效结构图、校正后的系统框图、设计出的网络);(c) 题目没有给图,而示意图能显著帮助理解。**题目已经把结构图、电路图、原理图作为已知条件给出时,直接引用题号与环节名称即可,不要重画一遍**——重画既多余又容易画错。
   - 要画时覆盖的图种:原理方框图、传递函数框图、电路图、波形图、相量图、受力图、积分区域、三维立体图、函数图像。图可以出现在答卷段末尾或对应小问处。
   - **不要用 ASCII 字符拼方框图**——手拼的框图既不好看也不够准,只在本机确实无法运行绘图脚本时才允许作为兜底,并在图前明确写「渲染环境不支持绘图,以下为文字框图」。
6. **题目点名要求的内容必须进第一段,但只写陈述**——凡题目明确要求回答的对象(工作原理、工作过程、判据、量值、关系式)一律写进答卷段,**写法限于「是什么、怎么流、等于多少」**;原因、依据、稳定性论证、方案对比、延伸讨论一律留到第二段。以「分析工作原理」为例:第一段只写信号流向(检测 → 比较 → 放大 → 执行 → 调节 → 反馈),不写「为什么能自动稳定」「为什么采用电枢控制」这类分析。
7. **公式用 LaTeX**——行内 `$...$`、独立成行 `$$...$$`。本 GUI 支持 KaTeX 渲染,**裸文本公式(如 `∫₀^{2π}`)不会被排版**,必须写成 `$\int_{0}^{2\pi}$`。数学、物理类答卷天然一行一个等式,可以分行,每行只写一个等式或一个结论;仍然禁止小标题、加粗和表格。

长度按**分值**锚定,不按字数:

| 题目分值 | 答卷段篇幅 |
|---|---|
| 小问、2 分以内 | 1–2 句 |
| 5 分左右 | 3–5 句 |
| 8–10 分大题 | 5–8 句,可附一张图 |

**多小问题目按各问分值分别给篇幅**,不要因为总题号大就整体灌水,也不要把五个小问压成一段。**图不计入句数限制**。题目未标注分值时,默认按中档处理。

不因为题目简单而灌水,也不因为题目难而漏掉得分点。**计算题宁可多留一行公式,也不要多写一段算术。**

## 二、解析的写法

这一段可以分点、可以用标题、可以长,不受第一段任何限制。**解析段同样建议按小问编号组织**,便于与答卷段逐条对照。

- 逐层展开:为什么这样答、每一步的依据、关键公式的来源与适用条件——第一段刻意省掉的「为什么」全部在这里补。
- 指出易错点:典型错答、单位与符号陷阱、前提条件、与相近概念的区分。
- 给出通法:同类题的解题套路,必要时补充相关定理。
- 中间被跳过的算术、代数化简,可以在这一段补完,方便学生核对。
- 若第一段有拿不准的地方,在这里说明假设与影响。

## 绘图规范

本机工具链:Python 3.13 + matplotlib 3.11 + numpy 2.4;MATLAB R2024b 可用但启动慢,非首选。**没有** graphviz、mermaid-cli、LaTeX。本 GUI 支持 KaTeX 公式,但**不支持 Mermaid**,流程类图不要指望它。

### 图片怎么让用户真正看到(关键)

**本地图床(`http://127.0.0.1:...`)只在当前这台机器的浏览器里有效**:换个客户端(网页版 AI、手机、别人的电脑)打不开,图片文件一旦从临时目录清掉,历史对话里的图也会变成空白占位。所以**默认走公网链接**。

固定三步:

1. **画图** → 输出到临时目录 `$env:TEMP\dsh-figs`(脚本与 PNG 都不写进用户的资料目录)。
2. **上传** → 用本 skill 自带脚本推送到本仓库的 `figs/` 目录,拿到公网链接:

   ```powershell
   python "$env:USERPROFILE\.dsh\skills\exam-answer-first-cn\assets\publish_fig.py" <png路径> --name <英文短名>
   ```

   脚本会打印 jsDelivr 与 raw 两个链接,并自动给文件名加时间戳;**jsDelivr 国内可直连,优先用**。
3. **内联** → 回答里写 `![说明](jsDelivr链接)`。

约束与兜底:

- 图宽 700–900 px、单文件 ≤ 300 KB(`publish_fig.py` 会自动压缩超限的图)。
- 文件名由脚本自动加时间戳,避免 CDN 缓存旧图。
- **上传失败时**(无网络、无 git 凭证、推送被拒):退化为本地图床——

  ```powershell
  $d = Join-Path $env:TEMP "dsh-figs"
  python -m http.server 8765 --bind 127.0.0.1 --directory $d   # 后台运行
  ```

  并在回答里**明确写「该链接仅在本机有效」**,不要让用户以为换个客户端也能看。
- 图会公开在 GitHub 仓库里——这是让图片在任何客户端都能显示、且历史对话长期有效的代价;若用户要求隐私,可改用私有仓库,但私有仓库的链接对外部客户端无效,那时只能退回本地图床。

### 写脚本时的固定要点

- 中文字体:`plt.rcParams["font.sans-serif"] = ["Microsoft YaHei"]`,`plt.rcParams["axes.unicode_minus"] = False`。
- **matplotlib 不做字体回退**:雅黑缺 `⊖`(U+2296)等字形,直接写会渲染成方块。公式符号一律走 mathtext(`$\oplus$`、`$\ominus$`、`$\partial$`、`$\theta$`、`$u_f$`);mathtext **不支持 `\dfrac`**,用 `\frac`。
- **控制成图尺寸(重要)**:内联显示的图**宽度控制在 700–900 px**。做法是 `figsize` 与 `dpi` 配合,例如 `figsize=(7.4, h)` + `dpi=100` ≈ 740 px 宽;超过约 1000 px 会在对话里占满整屏、观感很差。缩小 `figsize` 时要同步等比缩小字号与线宽,否则字会显得过大。
- **文件名只用 ASCII**(如 `autocontrol-3-20-structure.png`),避免 URL 编码问题。
- 框图:`FancyBboxPatch` 画圆角框 + `FancyArrowPatch` 画箭头,**比较点用 `$\oplus$ $\ominus$` 标极性**,反馈线从输出端绕回比较点。现成模板见 `assets/block_diagram.py`,改 `NODES` / `FEEDBACK_NODE` / 标签即可。
- **结构图/原理图含多个子图时用 `plt.subplots` 分列**,标题用 `ax.set_title`(字号够大才能看清 `\frac` 之类的公式),不要靠手写 y 偏移排布,否则子图会挤在一起。
- 立体图形:`mplot3d` 的 `plot_surface` + 交线 + 半透明顶盖;区域图:极坐标网格射线 + `fill` 高亮微元;波形图:直接画解析式曲线并标注相位、幅值、周期。
- 输出 PNG;需要打印或放进复习资料时另存 SVG(矢量放大不糊,但不要靠加大 PNG 尺寸来"高清")。
- **出图后必须自己渲染检查一遍再交付**(用图像查看确认中文、符号、对齐、标注位置、整体尺寸都正常);三维图的文字标注极易压住坐标轴或曲面,说明文字放图外(`fig.text` + `bbox_inches="tight"`),图内只留最必要的符号。
- **自检时逐项核对拓扑**:每个环节框、**框与框之间的连线与箭头**、反馈路径、极性标记、参数符号都要与原题一致。默认脚本里的连接语句不能只写在某个分支里(`if/else` 漏一个分支就会让信号链断开),**看到"框和框之间空着没箭头"必须当作错误重画**,不要因为图"看起来整齐"就放过。
- 图要能直接看出结论(标出交线、阴影区域、面积微元、关键点、极性),不做装饰图。
- **不要把绘图脚本或图片路径写进回答**——用户不需要留存这些文件,要交付的就是聊天里已经显示出来的那张图。脚本放临时目录,用完即弃。

## 正例与反例

### 例 1(原理题 + 方框图)

题目:图 1-17 为工业炉温自动控制系统的工作原理图。分析系统的工作原理,指出被控对象、被控量和给定量,画出系统方框图。

**✗ 不合格**(标签冒号式罗列、混入解析、用 ASCII 手拼框图):

> 被控对象:电炉;被控量:炉温;给定量:给定电压。因为热电偶检测炉温后与给定电压比较,偏差经放大驱动电动机……方框图给定电压→[电压放大]→[功率放大]→[电动机]→[电炉]

**✓ 合格**(一个小问内的三个要素合句;按步骤提行;图用真图):

> 该系统的被控对象为电炉,被控量为炉温 $T$,给定量为给定电压 $u_g$。
>
> 系统为按偏差控制的闭环负反馈系统:热电偶把炉温转换为电压 $u_f$,与给定电压 $u_g$ 比较得偏差电压
>
> $$u_e=u_g-u_f$$
>
> $u_e$ 经电压放大、功率放大后驱动直流电动机 M,电动机经减速器带动调压器改变加在电阻丝上的电压,使炉温向给定值变化。当 $u_f=u_g$ 时电动机停转,系统处于平衡状态,炉温稳定在给定值。
>
> 系统方框图如下:
>
> ![炉温自动控制系统方框图](http://127.0.0.1:8765/autocontrol-1-3-block.png)

### 例 1b(多小问大题 —— 必须分点)

题目:某系统 $G(s)=\dfrac{144}{s(s+10)}$,单位负反馈。(1) 求原系统的 $\zeta$、$\omega_n$ 及阶跃响应超调量;(2) 引入 $G_c(s)=aT_d s+1$,令 $\zeta=1$,求 $aT_d$。

**✗ 不合格**(把小问揉成一段):

> 原系统 $\omega_n=12$、$\zeta=0.417$、$\sigma\%=23.7\%$;引入 PD 后特征方程为 $s^2+(10+144aT_d)s+144=0$,令 $\zeta=1$ 得 $aT_d=7/72$。

**✓ 合格**(按小问分点,问内合句):

> (1) $\Phi(s)=\dfrac{144}{s^{2}+10s+144}$,$\omega_n=12\ \mathrm{rad/s}$,$\zeta=0.417$,$\sigma\%=23.7\%$。
>
> (2) 令 $\zeta=1$,由 $10+144aT_d=2\omega_n=24$ 得 $aT_d=\dfrac{7}{72}\approx0.097$,闭环极点为 $s_{1,2}=-12$。

### 例 2(数值计算题)

题目:质量 $m = 2\ \mathrm{kg}$ 的物体,所受合力 $F = 10\ \mathrm{N}$,求其加速度 $a$。

**✗ 不合格**(把算术和废话写进卷面):

> 根据牛顿第二定律可以知道,合力等于质量乘以加速度,所以把数字代进去,就是要用 10 去除以 2,算出来等于 5,因此 a = 5 m/s²,注意单位是米每二次方秒。

**✓ 合格**:

> 由牛顿第二定律 $F=ma$,得 $a=F/m=10/2=5\ \mathrm{m/s^2}$

### 例 3(符号计算题)

题目:电源电动势 $E$、内阻 $r$,外接电阻 $R$,求 $R$ 上的功率表达式及使功率最大的 $R$ 值。

**✓ 合格**:

> $P=I^{2}R=\left[\dfrac{E}{R+r}\right]^{2}R$
>
> 令 $\dfrac{\mathrm{d}P}{\mathrm{d}R}=0$,得 $R=r$,此时 $P_{\max}=\dfrac{E^{2}}{4r}$

### 例 4(证明 / 推导题)

题目:验证 $u(x,y)=x^{2}-y^{2}+xy$ 是调和函数,并求满足 $f(0)=0$ 的解析函数 $f(z)=u+iv$。

**✓ 合格**:

> $\dfrac{\partial u}{\partial x}=2x+y,\ \dfrac{\partial^{2}u}{\partial x^{2}}=2;\ \dfrac{\partial u}{\partial y}=-2y+x,\ \dfrac{\partial^{2}u}{\partial y^{2}}=-2$,故 $\dfrac{\partial^{2}u}{\partial x^{2}}+\dfrac{\partial^{2}u}{\partial y^{2}}=0$,$u(x,y)$ 为调和函数。
> 由 $\dfrac{\partial u}{\partial x}=\dfrac{\partial v}{\partial y}$ 得 $v=2xy+\dfrac{y^{2}}{2}+C(x)$;由 $\dfrac{\partial v}{\partial x}=-\dfrac{\partial u}{\partial y}=2y-x$,得 $C'(x)=-x$,即 $C(x)=-\dfrac{x^{2}}{2}+C_{1}$。
> 由 $f(0)=0$ 得 $C_{1}=0$,故 $f(z)=u+iv=\left(1-\dfrac{i}{2}\right)z^{2}$。

### 例 5(需要三维真图的题)

题目:求由抛物面 $z=x^{2}+y^{2}$ 与平面 $z=1$ 所围立体的体积。

**✓ 合格**:

> $V=\displaystyle\int_{0}^{2\pi}\!\!\mathrm{d}\theta\int_{0}^{1}(1-r^{2})\,r\,\mathrm{d}r=2\pi\left[\dfrac{r^{2}}{2}-\dfrac{r^{4}}{4}\right]_{0}^{1}=\dfrac{\pi}{2}$
>
> ![抛物面所围立体](http://127.0.0.1:8765/solid_demo.png)

**✗ 不合格**:用字符硬拼立体形状,或只给公式不给图。

## 读题失败的兜底

图片模糊、反光、被裁切、图号缺失时:

1. 先说明哪几处看不清、影响哪一问;
2. 能确定的部分照常给出答卷版答案,不确定的部分写成「按……理解作答」的条件性答案;
3. 不猜题号、页码,尤其不猜图中连线关系和器件型号。结构图中的反馈系数、方框标注看不清时,按题目文字给出的符号作答,并在解析段说明这一读图判断。

## 学科适配

科目差异只影响写什么内容,不改变两段式输出契约。

- **工科课程**(自动控制原理、电路、模电、数电、信号与系统、单片机、电机拖动、传感器等):优先保证原理框图、电路结构、波形相位的正确性,框图与电路图一律出真图并标注极性/节点;含数值的按「公式 → 代入 → 结果」写,结果带单位。
- **物理类课程**:先写所用定律的公式,再写代入数值的一步,最后给带单位的结果;中间运算不写;受力图、运动图像、场分布一律出真图。
- **数学类课程**(复变函数、离散数学、概率论、高等数学等):证明与推导保留必要逻辑链,一行一个等式,纯代数化简可跳;求值类按三步式;积分区域、曲线草图、旋转体、曲面交线全部出真图。
- **概念问答题**:答卷段直接给要点式完整句子,解析段再补充展开与辨析。
